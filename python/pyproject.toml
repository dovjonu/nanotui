[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nanotui"
version = "0.1.0"
description = "nanotui python bindings"
requires-python = ">=3.9"
